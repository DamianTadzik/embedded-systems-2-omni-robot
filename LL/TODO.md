increase the baud rate to 115200 (LL, L1).
handle telemetry on the jettson side (plotting?).
tune the regulator, loop time can be smaller like 10ms?
telemetry can be send every 10 or 20 or 100 ms too (need to calculate if it is possible actually)
telemetry data should be CSV like, and floats should be limited to 3 decimal places?
